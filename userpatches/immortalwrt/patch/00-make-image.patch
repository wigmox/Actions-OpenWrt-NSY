diff --git a/target/linux/rockchip/image/Makefile b/target/linux/rockchip/image/Makefile
index 4936998a78..b1ed9b78c0 100644
--- a/target/linux/rockchip/image/Makefile
+++ b/target/linux/rockchip/image/Makefile
@@ -15,7 +15,7 @@ define Build/boot-common
 	# and the kernel image (as kernel.img)
 	rm -fR $@.boot
 	mkdir -p $@.boot
-
+	$(CP) $(KDIR)/image-$(firstword $(DEVICE_DTS)).dtb $@.boot/rockchip.dtb
 	$(CP) $(IMAGE_KERNEL) $@.boot/kernel.img
 endef
 
@@ -66,7 +66,7 @@ endef
 ### Devices ###
 define Device/Default
   PROFILES := Default
-  KERNEL = kernel-bin | lzma | fit lzma $$(KDIR)/image-$$(firstword $$(DEVICE_DTS)).dtb
+  KERNEL = kernel-bin
   KERNEL_LOADADDR :=
   BOOT_SCRIPT :=
   BOOT_FLOW :=
diff --git a/target/linux/rockchip/image/default.bootscript b/target/linux/rockchip/image/default.bootscript
index 70be864831..46123c81f0 100644
--- a/target/linux/rockchip/image/default.bootscript
+++ b/target/linux/rockchip/image/default.bootscript
@@ -16,6 +16,7 @@ fi;
 
 setenv bootargs "coherent_pool=2M console=tty1 console=${serial_port},1500000 earlycon=uart8250,mmio32${serial_addr} root=PARTUUID=${uuid} rw rootwait";
 
+load ${devtype} ${devnum}:1 ${fdt_addr_r} rockchip.dtb
 load ${devtype} ${devnum}:1 ${kernel_addr_r} kernel.img
 
-bootm ${kernel_addr_r}
+booti ${kernel_addr_r} - ${fdt_addr_r}
